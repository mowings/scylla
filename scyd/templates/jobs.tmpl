<ol class="breadcrumb">
  <li class="active">jobs</li>
</ol>
<table class="table">
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Schedule</th>
    <th>Last Run</th>
    <th>Status</th>
    <th>Running?</th>
  </tr>
  {{$helpers := .Helpers}}
  {{ range .Jobs }}
  <tr>
   <td><a href="/jobs/{{.Name}}">{{ .Name }}</a></td>
   <td>{{ .Description}}</td>
   <td>{{ .Schedule }}</td>
   <td> {{$helpers.DisplayAgo .EndTime}}</td>
   <td> {{$helpers.DisplayRunStatus .LastRunStatus}}</td>
   <td> {{$helpers.DisplayBool .Running}} </td>
  </tr>
  {{ end }}
</table>
